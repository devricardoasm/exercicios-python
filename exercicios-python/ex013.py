salario = float(input('Digite seu salário: '))
aumento = salario + (salario*0.15)

print(' Seu salário atual é de: {:.2f} reais \n Com o aumento, seu salário será de: {:.2f} reais'.format(salario, aumento))
